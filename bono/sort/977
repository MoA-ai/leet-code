class Solution:
    def sortedSquares(self, nums: List[int]) -> List[int]:
        results = []
        for num in nums:
            results.append(num**2)
        results.sort()
        return results
        